# Notion export future

Notion may hold project status, architecture, deployment gates, and non-sensitive tasks only. It must never receive health samples, metrics, reports, screenshots, symptoms, medication, FIT files, tokens, or user identifiers from the health system.

There is no automatic health-record synchronization in RC4. Any future status export must use a fixed sanitized schema and a separate opt-in connector, and must be independently reversible.
