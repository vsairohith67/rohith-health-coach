# Third-party notices

This private source distribution depends on the packages recorded in `pnpm-lock.yaml` and `services/fit-parser/uv.lock`. Their upstream licences remain applicable. Run `pnpm licenses list --json` and `uv tree --directory services/fit-parser` during legal release review.

Key ecosystems include Next.js/React, TypeScript, Supabase clients/CLI, Model Context Protocol SDK, Zod, Vitest/Playwright/Axe, FastAPI/Pydantic/Uvicorn, uv/Ruff/Pytest, and the official Python container image.

The Garmin FIT SDK, Apple frameworks, model weights, Supabase/Vercel/Netlify/FastAPI Cloud services, and provider SDK/binaries are not redistributed. Garmin SDK use requires owner acceptance of its licence. Model candidates retain their individual MIT, Apache-2.0, or Gemma terms.
